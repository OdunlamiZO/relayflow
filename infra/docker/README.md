# Local Infrastructure

`docker-compose.yml` currently provides:

- PostgreSQL 16
- Redis 7

PostgreSQL is the primary application database. Redis is reserved for queues, rate limiting, and short-lived coordination once needed.
